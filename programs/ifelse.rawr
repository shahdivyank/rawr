main() {
    // initialize var a
    int a;
    a = 56;

    // initialize var b
    int b; 
    b = 12;

    // initialize var c 
    int c; 
    c = 7;

    // if else statements - should print out 7 (variable c)
    if (a > b && a < 50) {
        rout(a);
    } else {
        rout(c);
    }
}