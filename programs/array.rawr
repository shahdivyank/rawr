//test cases for array 

main() {
    int arr[10];
    int a;
    int b;

    //result will print 7 
    a = 5;
    b = 2;
    arr[0] = a + b;
    rout(arr[0]);

    //result will print 20
    arr[1] = 20;
    rout(arr[1]);
    
    //result will be 100
    arr[2] = 100;
    rout(arr[2]);

    int temp;
    temp = arr[1] - arr[2];
    arr[3] = arr[0] * temp;
    rout(arr[3]);
}

