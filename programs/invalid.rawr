// to detect invalid identifiers

main() {
    int 1a;
    1a = 20; // can't start with a number
    int _b;
    _b = 30; // can't start with an underscore
    int c_;
    c_ = 45; // can't end with an underscore either
}


